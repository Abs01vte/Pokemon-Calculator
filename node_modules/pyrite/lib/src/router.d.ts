/// <reference types="mithril" />
import * as m from "mithril";
import { Component } from "./component";
export interface Route {
    path: string;
    component: Component<any> | typeof Component;
    abstract?: boolean;
    default?: string;
    routes?: Array<Route>;
    props?: m.Attributes & {
        [key: string]: any;
    };
}
export declare type Routes = Array<Route>;
export declare class Router {
    build(configRoutes: Routes, currentRoutes?: Routes, routes?: m.RouteDefs): m.RouteDefs;
    private addKeys(lastRoute, args);
    private createRoute(nextRoutes);
    private onmatch(lastRoute, args, newPath);
    private render(otherRoutes, lastRoute);
}
export declare const router: Router;
